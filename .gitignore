/cmdcode2api
/cmdcode2api-linux-amd64
/cmdcode2api-linux-arm64
/cc-gateway
config.yaml
usage.json
discord-alerts.json
*.exe
.oauth_state
.oauth_url
nul

# --debug writes full request bodies and SSE events, so these hold developer
# prompts and user source. Never commit them.
log.txt*
*.log
*.patch
changes_report.md

# webui-src build tooling/deps: internal/app/webui/dist (its build output) is
# committed, see internal/app/webui-src/README.md.
internal/app/webui-src/node_modules/
internal/app/webui-src/*.tsbuildinfo

# Local agent/tooling state, not part of the project.
.claude/
.serena/
.sisyphus/
